module alt.probes.SeventhReentryProbe where

-- History (U20 seventh re-entry probe): the counterexample required two live
-- crossings of the same anchor and selected a noncanonical alias for β-Λ.
-- U27's begin constructor requires the anchor to be absent from σ, so the
-- second begin and hence the old redex are unrepresentable.  The corresponding
-- single-crossing positive β-Λ preservation instance is checked in
-- `alt.ThetaRegression`.
