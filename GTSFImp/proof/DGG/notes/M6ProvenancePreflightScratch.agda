module M6ProvenancePreflightScratch where

-- File Charter:
--   * Historical notes scratch for the candidate-A M6 provenance preflight.
--   * LG-3 removes the CatchupCast/CatchupColumn family this scratch used to
--     calibrate, so the checked content is intentionally reduced to a
--     superseded marker.
--   * The live replacement obligations are recorded in
--     lg3-value-catchup-column-blocked.red: the M6 column recursion now needs
--     a CTI column-layer peel/inversion theorem instead of tail provenance.

open import Data.Unit using (⊤; tt)

lg3-m6-provenance-preflight-superseded : ⊤
lg3-m6-provenance-preflight-superseded = tt
