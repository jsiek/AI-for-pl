Example 12. Up and then down.

    ∅ ⊢ (id                  : ∀X.X→X
            ⟨ ν̅α.α♯→α♭ ⟩      : ★→★
            ⟨ να.α!→α? ⟩      : ∀X.X→X
            ) ι                : ι→ι
            c                 : ι
            ⊒ id ι c : id_ι


    id ⇓ id



     --- coerceValue (instᶜ ★ (α♯→α♭)) id
     --- instantiateValue α id = λx:α.x
     --- coerceValue (α♯→α♭) (λx:α.x) = (λx:α.x) ⟨α♯→α♭⟩ 
    (id ⟨ ν̅α.α♯→α♭ ⟩) ⇓ {α := ★} , (λx:α.x) ⟨α♯→α♭⟩ 


    --- coerceValue (genᶜ ★ (α!→α?)) ((λx:α.x) ⟨α♯→α♭⟩)
    (id ⟨ ν̅α.α♯→α♭ ⟩ ⟨ να.α!→α? ⟩) ⇓ ((λx:α.x) ⟨α♯→α♭⟩ ⟨ να.α!→α? ⟩)

    c ⇓ c)

    ----------------------------------------
    (id ⟨ ν̅α.α♯→α♭ ⟩ ⟨ να.α!→α? ⟩) ι c ⇓ ___

%%% above is not correct because type app per-se is compiled away.

LHS

    να:=ι. ((id ⟨ ν̅α.α♯→α♭ ⟩ ⟨ να.α!→α? ⟩) α ⟨α#→α♭⟩) 42

--> interpret ∅ (id ⟨ ν̅α.α♯→α♭ ⟩ ⟨ να.α!→α? ⟩)
--> {β:=★} , ((λx:β.x) ⟨β♯→β♭⟩ ⟨ να.α!→α? ⟩)
--> instantiateValue {β:=★, α:=ι} α ((λx:β.x) ⟨β♯→β♭⟩ ⟨ να.α!→α? ⟩)
    --> coerceValue  {β:=★, α:=ι} (α!→α?) ((λx:β.x) ⟨β♯→β♭⟩)
    -->  {β:=★, α:=ι} , ((λx:β.x) ⟨β♯→β♭⟩ ⟨α!→α?⟩ )

    coerceValue _ (α#→α♭) $$
    -->  {β:=★, α:=ι} , ((λx:β.x) ⟨β♯→β♭⟩ ⟨α!→α?⟩ ⟨α#→α♭⟩)

--> interpret {β:=★, α:=ι} , ((λx:β.x) ⟨β♯→β♭⟩ ⟨α!→α?⟩ ⟨α#→α♭⟩) 42
    (β# (α! (α# 42 : α) : ★) : β)
    --> (λx:β.x)
    (β♭ (β# (α! (α# 42 : α) : ★) : β) : ★)
    --> (α! (α# 42 : α) : ★)
    --> (α? (α! (α# 42 : α) : ★) : α)
    --> (α# 42 : α)
    --> (α♭ (α# 42 : α))
    --> 42 : ι

RHS


    να:=ι. (id α ⟨α#→α♭⟩) 42
    
