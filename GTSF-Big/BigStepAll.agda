module BigStepAll where

-- File Charter:
--   * Aggregate check for the GTSF big-step design draft.
--   * Imports the semantics, observations, adequacy boundary, and DGG
--     statement so one Agda invocation checks the complete public draft.

import BigStep
import BigStepExamples
import BigStepObservations
import BigStepProperties
import BigStepDynamicGradualGuarantee
