module InterpreterMilestoneTwo where

-- File Charter:
--   * Focused aggregate for concrete world, environment, and value narrowing.
--   * Checks world extension, name substitution, joined values, and examples.
--   * Contains no reduction semantics or reduction-derived result.

import InterpreterWorldNarrowing
import InterpreterWorldNarrowingProperties
import InterpreterValueNarrowing
import InterpreterEnvironmentNarrowing
import InterpreterValueSubstitution
import InterpreterJoined
import InterpreterValueNarrowingExamples
